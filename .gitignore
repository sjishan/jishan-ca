# Local-only context for Claude Code — present on disk, never committed or deployed
CLAUDE.md
brand-style.md

# Build output (regenerated by `npm run build`)
dist/

# Node
node_modules/
npm-debug.log*
.npm

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
